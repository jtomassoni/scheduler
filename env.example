# ============================================
# REQUIRED ENVIRONMENT VARIABLES
# ============================================

# Database Connection
# Format: postgresql://user:password@host:port/database
# Example: postgresql://postgres:password@localhost:5432/scheduler
DATABASE_URL=postgresql://user:password@host:5432/database

# NextAuth Configuration
# Your application URL (update after first Vercel deploy)
# For local development: http://localhost:3000
# For Vercel: https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000

# NextAuth Secret
# Generate with: openssl rand -base64 32
# Or use: https://generate-secret.vercel.app/32
NEXTAUTH_SECRET=your-nextauth-secret-key-here-generate-a-random-32-byte-string

# ============================================
# OPTIONAL: Push Notifications
# ============================================
# Only needed if you want to enable push notifications
# Generate VAPID keys with: npx web-push generate-vapid-keys

# VAPID Public Key (starts with BL...)
# This is safe to expose publicly
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key-here

# VAPID Private Key (keep secret!)
# This must be kept private
VAPID_PRIVATE_KEY=your-vapid-private-key-here

# VAPID Subject
# Your email address in mailto: format
VAPID_SUBJECT=mailto:your-email@example.com

# ============================================
# OPTIONAL: Emergency Fallback Admin
# ============================================
# Fallback superadmin account that works even if database is unavailable
# This account bypasses database authentication and is always available
# 
# Option 1: Single fallback admin
FALLBACK_ADMIN_USERNAME=your-username
FALLBACK_ADMIN_PASSWORD=your-secure-password

# Option 2: Multiple fallback admins (comma-separated, format: username:password)
# FALLBACK_ADMINS=admin1:password1,admin2:password2

# ============================================
# OPTIONAL: Email Service (Future)
# ============================================
# These are not currently used but reserved for future email integration

# SMTP Configuration for Email Notifications
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourdomain.com

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file to .env.local (for local development)
#    cp env.example .env.local
#
# 2. Fill in all REQUIRED variables
#
# 3. For Vercel deployment:
#    - Go to your Vercel project settings
#    - Navigate to Environment Variables
#    - Add each variable from the REQUIRED section above
#    - Optionally add OPTIONAL variables if needed
#
# 4. After first Vercel deploy:
#    - Update NEXTAUTH_URL with your actual Vercel URL
#    - Example: https://scheduler-abc123.vercel.app
#
# 5. Generate secrets:
#    - NEXTAUTH_SECRET: openssl rand -base64 32
#    - VAPID keys: npx web-push generate-vapid-keys

